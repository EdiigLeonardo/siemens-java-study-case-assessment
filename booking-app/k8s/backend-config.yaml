apiVersion: v1
kind: ConfigMap
metadata: { name: backend-config }
data:
  DB_HOST: "postgres"
# BUG: `kubectl apply` a um ConfigMap nao reinicia os pods que o usam -
# sem um hash/annotation no template do Deployment, a app fica a correr
# com a config antiga ate alguem apagar os pods a mao.
